services:
  mysql:
    image: mysql:8.0
    container_name: aquacare-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: aquacare
      MYSQL_USER: aquacare
      MYSQL_PASSWORD: aquacare123
    ports:
      - "3306:3306"
    volumes:
      - aquacare-data:/var/lib/mysql

volumes:
  aquacare-data: